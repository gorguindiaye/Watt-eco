import React from 'react';
import ReactDOM from 'react-dom/client';
import WattEcoApp from '../watt-eco-app.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WattEcoApp />
  </React.StrictMode>
);
